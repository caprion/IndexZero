# Eval

The shared evaluation harness will land in M4.

Until then, keep this folder reserved for:

- benchmark fixtures
- metric helpers
- later module-wide evaluation code

M1 only needs local correctness tests, which live under `tests\`.
